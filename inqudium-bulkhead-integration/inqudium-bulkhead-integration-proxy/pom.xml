<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>eu.inqudium</groupId>
        <artifactId>inqudium-bulkhead-integration</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>inqudium-bulkhead-integration-proxy</artifactId>
    <name>Inqudium :: Bulkhead :: Integration Example :: Proxy</name>
    <description>
        JDK-dynamic-proxy bulkhead integration example. Demonstrates wrapping an
        OrderService interface implementation with InqProxyFactory so the bulkhead
        sits transparently between caller and implementation. Stub at sub-step 5.A;
        populated in 5.C.
    </description>

</project>
